# LLM Provider (optional, defaults to "nvidia")
# Options: "nvidia", "openai", "groq"
LLM_PROVIDER=nvidia

# NVIDIA API Key (required if using nvidia provider)
NVIDIA_API_KEY=your_nvidia_api_key_here

# OpenAI API Key (required if using openai provider)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model (optional, defaults to "gpt-4o")
# Options: "gpt-4o", "gpt-4o-mini", "gpt-4-turbo", "gpt-3.5-turbo", etc.
OPENAI_MODEL=gpt-4o

# Groq API key (required for FAST ASR or Groq LLM)
GROQ_API_KEY=your_groq_api_key_here

# API Base URL for frontend (optional, defaults to http://localhost:8000)
NEXT_PUBLIC_API_BASE=http://localhost:8000